import React from 'react'
import ReactDOM from 'react-dom/client'
import PolygonPathfinding from './components/PolygonPathfinding'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PolygonPathfinding />
  </React.StrictMode>
)